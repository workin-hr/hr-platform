# This asset has moved

The authoritative ADR template is `docs/adr/ADR-0000-template.md`. It used to
be duplicated here with a different (drifted) structure, which is exactly how
`docs/adr/README.md`'s eight real ADRs ended up using a `## Proposed
Direction` heading that the validator required as `## Decision` — two copies
of the same template drifted apart. There is now only one authoritative copy.

Use `docs/adr/ADR-0000-template.md` as the starting point when running this
skill (see `../SKILL.md`, step 1).
