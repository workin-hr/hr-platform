# PR Draft — 2026-08-05

## Target

- Base branch: `main`
- Compare branch: `docs/pre-migration-readiness-gap-analysis`
- Actual reviewed remote range: `origin/main..HEAD`

## Suggested Title

`feat(backend): begin Phase 1 backend implementation and close review findings`

## Suggested Description

```md
# Pull Request

## Summary

Starts the explicitly authorized Phase 1 backend slice under `backend/`
and closes the follow-up review findings raised on Wednesday, August 5,
2026. This PR lands the first real Spring Boot/Java backend
implementation for tenant identity, JWT auth, tenant-membership
validation, RLS wiring, and supporting tests, while also reconciling the
repo validator and governance/docs with the new post-D-028 state.

## Linked Reference

- Linked issue or approved bootstrap plan:
  - `docs/bootstrap/decision-log.md` D-028
  - `docs/migration/pre-migration-readiness-gap-analysis.md`
  - `evidence/change-review-2026-08-05.md`

## Scope

- Scope of this change:
  - lift the implementation lock for `backend/` only
  - add the first real backend auth/tenant-identity slice
  - add backend CI/dependabot/CODEOWNERS coverage for `backend/`
  - fix review findings around validator behavior, JWT hardening,
    runtime credential handling, and stale governance docs

## Files Changed

- Key files changed:
  - `backend/**`
  - `.github/workflows/backend-validate.yml`
  - `.github/dependabot.yml`
  - `CODEOWNERS`
  - `scripts/validate_phase0.py`
  - `scripts/test_validate_phase0.py`
  - `README.md`
  - `docs/architecture/authorization-model.md`
  - `docs/migration/consolidated-task-matrix.md`
  - `docs/migration/pre-migration-readiness-gap-analysis.md`
  - `evidence/change-review-2026-08-05.md`

## Decisions Made

- Decisions encoded by this PR:
  - `backend/` is the only unlocked implementation directory at this time
  - initialized git submodule content under declared submodule paths is
    treated as an external repo boundary, not as `hr-platform` product
    code
  - backend startup must fail closed if the old placeholder JWT secret is
    used
  - JWT parsing must reject wrong-audience or missing-claim tokens before
    request handling reaches tenant context logic
  - runtime DB credentials must be environment-provided, not hardcoded in
    code or SQL
  - ambiguous multi-tenant login is rejected until explicit tenant
    selection exists

## Assumptions

- Assumptions made:
  - Flyway placeholder substitution is available for the runtime-role SQL
    migration
  - rejecting multi-membership login with `409 Conflict` is acceptable as
    an interim fail-closed behavior until explicit tenant selection is
    implemented
  - the compare branch for this PR is `origin/main`, not the stale local
    `main` ref

## Validation

- Commands executed:
  - `python3 scripts/validate_phase0.py`
  - `python3 scripts/test_validate_phase0.py`
  - `python3 scripts/test_git_guard.py`
- Results:
  - all three passed locally on Wednesday, August 5, 2026
  - `backend/gradlew test` could not be executed in this environment
    because the only discovered SDKMAN Java binary fails with
    `Permission denied`; backend CI or a machine with a working JDK must
    run the Gradle suite before merge

## Evidence

- Supporting evidence and references:
  - `docs/bootstrap/decision-log.md` D-028
  - `docs/adr/ADR-0002-modular-monolith-baseline.md`
  - `docs/adr/ADR-0010-authorization-model.md`
  - `evidence/change-review-2026-08-05.md`

## Impact

- Security impact:
  - improves auth hardening by removing the default JWT-secret fallback,
    validating required token claims, and removing hardcoded runtime DB
    credentials
- Architecture impact:
  - begins the first real backend implementation of the accepted auth/RLS
    direction
- Migration impact:
  - updates migration-readiness docs to reflect the real D-028 backend
    start and the remaining planning gates accurately
- API impact:
  - introduces initial `/api/auth/*` and `/api/tenant/me` backend
    behavior in the new backend slice

## Rollback Or Reversal

- Reversal approach:
  - revert this PR to restore the pre-D-028 state on `main`
  - if only the follow-up review fixes need reversal, revert commit
    `45816ac`

## Unresolved Items

- Remaining open questions or deferred items:
  - explicit tenant selection flow for identities with multiple active
    memberships
  - full backend Gradle/test verification in an environment with a
    working JDK
  - broader module-by-module migration execution planning beyond the
    already authorized auth/tenant-identity slice

## Scope Guardrail

- [x] No unauthorized application implementation was added.
- [x] No secrets or credentials were added.
```

## Human Steps

1. Run `backend/gradlew test` in a machine or CI environment with a
   working Java runtime.
2. Push `docs/pre-migration-readiness-gap-analysis`.
3. Open a PR to `main` using the title and description above.
4. Merge only after backend CI/test verification succeeds.
