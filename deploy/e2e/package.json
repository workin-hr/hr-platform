{
  "name": "workin-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end tests against the deployed Workin stack",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
