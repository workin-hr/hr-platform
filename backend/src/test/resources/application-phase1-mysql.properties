# The legacy production contract uses non-strict MariaDB write semantics. Hikari runs this for
# every physical application connection, so E2E requests and fixture setup no longer use different
# sql_mode values. This is test-only and does not alter production configuration or schema/data.
app.legacy-db.connection-init-sql=SET SESSION sql_mode=''
