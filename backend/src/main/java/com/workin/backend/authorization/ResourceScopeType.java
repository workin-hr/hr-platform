package com.workin.backend.authorization;

/**
 * V31's scope_type CHECK values. BRANCH/DEPARTMENT only this slice;
 * DIRECT_REPORT and EMPLOYEE (F-25) deferred.
 */
public enum ResourceScopeType {
	BRANCH,
	DEPARTMENT
}
