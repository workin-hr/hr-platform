# Spec Kit Integration Placeholder

This repository reserves `.specify/` for Spec Kit artifacts and integrations.

Planned bootstrap commands:

- `uv tool install specify-cli`
- `specify version`
- `specify init . --integration claude`
- `specify integration install codex`
- `specify integration list`
- `specify integration status`

During Phase 0, use specification and planning commands only. Do not use implementation commands for product code.
