"""WorkIn devices agent: reads attendance terminals on a branch LAN and delivers their punches.

Also the field kit for a site visit (scan, read-only terminal info, a recording proxy) and the
simulators the local lab uses in place of hardware. See devices-agent/README.md.
"""

VERSION = "0.1.0"
