# Consolidated Task Matrix

## Purpose

Converts every finding and open question produced by Discovery so far —
the 24 `hr-legacy` security/correctness findings, the 8 `hr-platform`
pre-migration readiness gaps (PMR-01..10, minus PMR-05/06 which are
tracked as sub-items of PMR-06 rather than standalone GitHub issues), and
the 11 new findings from the Flutter client Discovery pass — into one
place with a consistent field set, so nothing has to be rediscovered by
reading prose across five different documents. This document does not
replace the source documents (`docs/security/threat-model.md`,
`docs/legacy/business-rule-extraction.md`,
`docs/migration/pre-migration-readiness-gap-analysis.md`,
`docs/api/flutter-request-response-compatibility.md`) — it indexes them.

This is a planning artifact. It does not itself close, fix, or implement
anything, per this repository's [`CLAUDE.md`](../../CLAUDE.md) boundary.

## Field Definitions

- **Severity**: technical/business impact if the finding is never
  addressed (`Critical`/`High`/`Medium`/`Low`), inherited from the
  originating issue where one exists.
- **Priority**: sequencing recommendation independent of severity —
  a `Low`-severity item can still be `P0` if it blocks something else
  (e.g. an orphaned-endpoint question that must be answered before
  writing an OpenAPI contract for that module).
  - `P0` — blocks the technical spike or any backend implementation start
  - `P1` — blocks cutover of the specific module it concerns
  - `P2` — should be designed/fixed correctly during the rewrite, not
    independently blocking
  - `P3` — backlog / opportunistic, no migration dependency
- **Owner Type**: who must act to close this — `Engineering` (write code
  or a technical design), `Product` (make a scope/priority call),
  `Security` (approve a remediation approach or verify a live control),
  or `Manual Operator` (perform a one-time action outside any codebase,
  e.g. a GCP Console check or a credential-custody confirmation).
- **Blocking?**: `Blocking` (names what it blocks) or `Non-blocking`.
- **Evidence For Closure**: what must exist for this row to be marked
  closed — not "fixed the bug" in the abstract, but the concrete artifact
  (a merged PR against the Java rewrite with a specific test, a signed-off
  ADR, a screenshot, a product decision recorded in writing).
- **Dependency / Target Milestone**: what has to happen first, and which
  named milestone from the
  [Migration-Readiness Gate](./pre-migration-readiness-gap-analysis.md#migration-readiness-gate)
  this row is tied to.

## Section A — `hr-legacy` Security And Correctness Findings (Issues #2–#25)

**Policy, revised 2026-08-04**: these are fixed in the new platform's
implementation, not patched into the legacy PHP system, unless one
specifically represents an **immediate production-critical risk
requiring emergency action independent of migration timing** (the one
candidate for that exception is `#4`, flagged individually below — its
severity depends entirely on an unconfirmed live production fact).
Legacy security findings **do not automatically block starting new
backend implementation** — they become **mandatory acceptance criteria**
for the equivalent new module, verified by an automated test before that
module is considered done, not a gate on beginning work. Every finding
below remains permanently recorded and traceable regardless of this
reframing: GitHub issue, this matrix, and (for the four
tenant-isolation-class findings) explicit design requirements are noted
per row.

All 24 filed 2026-08-04, `bug` label, repo `workin-hr/hr-legacy`. Full
finding text lives in the issues themselves and in
`docs/security/threat-model.md` / `docs/legacy/business-rule-extraction.md`.

| # | Title | Severity | Priority | Owner Type | Blocking? | Evidence For Closure | Dependency / Target Milestone |
|---|---|---|---|---|---|---|---|
| [#2](https://github.com/workin-hr/hr-legacy/issues/2) | Any HR-role user can delete/suspend/view any other company platform-wide | Critical | P1 (acceptance criterion, not a start-blocker — reclassified 2026-08-04) | Engineering + Security | **Non-blocking for implementation start.** Mandatory acceptance criterion for the employees/company-admin Java module: fixed as part of building it, not before starting | Java tenant-isolation module has an automated test proving cross-tenant access is rejected for this exact operation class | Before that module's cutover, not before implementation begins |
| [#3](https://github.com/workin-hr/hr-legacy/issues/3) | Cross-tenant employee password takeover via dashboard employee edit | Critical | P1 (acceptance criterion) | Engineering + Security | **Non-blocking for implementation start.** Mandatory acceptance criterion for the employees module. **Progress 2026-08-07**: the new employees admin surface fixes this structurally — the `Employee` entity never maps `password_hash` and no DTO carries credentials (credentials live on `identities`, reachable only via auth flows), with cross-tenant mutation proven a 404 (`EmployeeModuleFlowTest`). Full closure when the remaining employee-edit surface (role/lifecycle) ships with the same guarantees | Same as #2 — tenant-isolation test suite covers password-mutation endpoints specifically; credential-free surface proven by test | Before that module's cutover |
| [#4](https://github.com/workin-hr/hr-legacy/issues/4) | OTP code returned in API response when `DEBUG=true` | Critical — was confirmed live 2026-08-04; **live value remediated on production 2026-08-05** (`PMR-05` closed) | P1 (acceptance criterion — the live incident that made this row P0 is closed) | Engineering + Security | **Non-blocking since 2026-08-05.** The live account-takeover path was closed when the repository owner confirmed the production `DEBUG` value was changed (see `PMR-05` in [`pre-migration-readiness-gap-analysis.md`](./pre-migration-readiness-gap-analysis.md) and `docs/bootstrap/decision-log.md` D-028's preconditions). What remains is the standard new-system acceptance criterion: the Java rewrite must never echo an OTP under any config flag | Live remediation: recorded closed 2026-08-05 (`PMR-05`). Remaining closure: Java OTP-verification code path has an automated test proving no OTP value appears in any API response regardless of configuration | New-system fix: before auth/OTP module cutover |
| [#5](https://github.com/workin-hr/hr-legacy/issues/5) | Cross-tenant IDOR in API `advances` module | High | P1 (acceptance criterion) | Engineering + Security | **Non-blocking for implementation start.** Mandatory acceptance criterion for the advances module. **Progress 2026-08-07**: the new advances module's F-18 suite (`AdvanceModuleFlowTest`) proves cross-tenant get/approve/reject and create-with-foreign-employee are indistinguishable 404s; pay and delete deliberately do not exist in the surface yet (pay is payroll's finalize side effect; delete awaits the lifecycle/retention decision) and get the same tests when they arrive. **Update 2026-08-07**: the pay side effect now exists — payroll finalize deducts from APPROVED advances — and inherits batch scoping: cross-tenant finalize on a foreign batch is a 404 (`PayrollModuleFlowTest`); a direct per-advance pay endpoint still does not exist | Same tenant-isolation test suite covers `advances` approve/reject/pay/delete/create — approve/reject/create covered; pay/delete with their future slices | Before that module's cutover |
| [#6](https://github.com/workin-hr/hr-legacy/issues/6) | Cross-tenant IDOR across 10 dashboard modules | High | P1 (acceptance criterion) | Engineering + Security | **Non-blocking for implementation start.** Mandatory acceptance criterion for each of the 10 equivalent new modules | Tenant-isolation test suite parameterized across all 10 named modules | Before each module's cutover |
| [#7](https://github.com/workin-hr/hr-legacy/issues/7) | JWT valid 10 years; no admin token revocation; password change/reset never invalidates sessions | High | P1 (acceptance criterion — design decided 2026-08-04, reclassified from P0) | Engineering + Security | **Server side implemented 2026-08-06** for both token domains: short-lived access tokens plus rotating opaque refresh tokens with reuse-detection family revocation, logout revocation, and revoke-all primitives (`backend/.../identity/RefreshTokenService.java`, `backend/.../platformadmin/PlatformAdminSessionService.java`, tests `RefreshTokenServiceTest`/`AuthSessionFlowTest`/`PlatformAdminSessionFlowTest`). Remaining for full closure: password-change/reset endpoints calling the revoke-all primitive when they are built (F-27), and the client-side workstream (F-02) | ADR-0005 `Accepted`; server-side refresh/rotation/revocation implemented per `docs/security/authentication-remediation-design.md`; remaining wiring tracked under F-27 and F-02 | Before auth module cutover |
| [#8](https://github.com/workin-hr/hr-legacy/issues/8) | `hr_permissions` matrix enforced on ~21 endpoints, ignored on ~130+ others | Medium | P1 | Engineering | Blocking: cutover of any permission-gated module | **Model decided 2026-08-05** (`docs/adr/ADR-0010-authorization-model.md`, Accepted): application-service method authorization is the single authoritative enforcement boundary, with an architecture test (F-23) that fails the build on any undeclared use case — structurally closes this issue's root cause, not just this one instance of it. Real enforcement code does not exist yet — tracked as F-15/F-17/F-23 | Before any module's cutover; catalog migration (F-15) and enforcement (F-23) implemented |
| [#9](https://github.com/workin-hr/hr-legacy/issues/9) | Unauthenticated `complete_company_registration.php` trusts a guessable `company_id` | Medium | P1 | Engineering | Blocking: cutover of company-onboarding module | New onboarding flow uses a non-guessable, single-use token instead of sequential ID | Before onboarding module cutover |
| [#10](https://github.com/workin-hr/hr-legacy/issues/10) | OTP verification has no rate limiting / brute-force protection | Medium | P1 | Engineering | Blocking: cutover of auth module | Rate limiting implemented and load-tested on OTP verify path in new system | Before auth module cutover |
| [#11](https://github.com/workin-hr/hr-legacy/issues/11) | Single shared platform-admin password — no per-admin identity, no MFA, no audit trail | High (decided, not accepted as new-platform architecture) | **P0** | Product + Security + Engineering | **Blocking production readiness and any privileged platform-admin operation** | **Decided 2026-08-05**: the new platform requires individual platform-admin identities (credentials, sessions, revocation, audit attribution) — not the shared-password model. Tracked as F-26 (`docs/migration/consolidated-task-matrix.md`), `docs/architecture/authorization-model.md` §7, `docs/bootstrap/decision-log.md` D-027 | Before any platform-admin functionality reaches production |
| [#12](https://github.com/workin-hr/hr-legacy/issues/12) | `payslips/create.php` silently drops base pay for daily-wage-mode employees | High | P1 | Engineering | Blocking: cutover of payroll module. **Progress 2026-08-07**: fixed by construction in the new payroll module — the manual add-one-payslip endpoint (the exact legacy defect site) routes through the same `PayrollCalculationService` as batch-wide calculate, proven by `PayrollBatchLifecycleTest.manuallyAddedPayslipForADailyWageEmployeeHasNonZeroBasePay` | Payslip-total calculation has one canonical implementation with a test covering daily-wage mode explicitly | Before payroll module cutover |
| [#13](https://github.com/workin-hr/hr-legacy/issues/13) | Three independent, divergent implementations of payslip-total math | Medium | P1 | Engineering | Blocking: cutover of payroll module. **Progress 2026-08-07**: the rewrite has exactly one implementation — `PayrollCalculationService.compute`, called by batch calculate and manual payslip create/update alike (`PayrollCalculationServiceTest`; `PayslipService`'s Javadoc records the defect-site lineage) | Single shared calculation function/service, unit-tested, used by every payslip code path | Before payroll module cutover |
| [#14](https://github.com/workin-hr/hr-legacy/issues/14) | `salary_contracts.housing_allowance` cannot be set to nonzero anywhere in the API | Medium | P2 | Engineering + Product | Non-blocking | Product confirms intended behavior (dead field vs. missing feature) before the new schema is finalized | Before payroll module schema freeze |
| [#15](https://github.com/workin-hr/hr-legacy/issues/15) | Mobile "logout" silently deactivates the employee's account | High | P1 | Engineering | Blocking: cutover of auth/employee-lifecycle module | New logout endpoint never mutates account-active status; regression test asserts this | Before auth module cutover |
| [#16](https://github.com/workin-hr/hr-legacy/issues/16) | QR check-in skips the 2-hour minimum-gap anti-fraud rule | Medium | P2 | Engineering + Product | Non-blocking (real-world exploitability depends on whether a third client uses it — see row F-04). **Note 2026-08-07**: the attendance first slice ships no QR surface, so nothing needs retrofitting when the decision lands; the new manual-entry surface already enforces the 2-hour gap (`AttendanceModuleFlowTest`) | Product confirms whether QR check-in has a live caller; if yes, promote to P1 and add the same anti-fraud gap check used by GPS/manual check-in | Depends on F-04 answer; before attendance module cutover if a live caller is confirmed |
| [#17](https://github.com/workin-hr/hr-legacy/issues/17) | Manager role gets unscoped company-wide attendance visibility despite doc-comments claiming department scoping | Medium | P2 | Engineering + Product | Non-blocking | Product confirms intended scoping; new system's manager-role query is scoped to match whichever answer is confirmed | Before attendance module cutover |
| [#18](https://github.com/workin-hr/hr-legacy/issues/18) | Manager can approve/reject any employee's leave/permission request company-wide | Medium | P2 | Engineering + Product | Non-blocking. **Note 2026-08-07**: the new requests module gates approve/reject behind `requests.approve` with the MANAGER bundle empty by default — structurally parked exactly like #17 until F-16/F-25's scoping decision; no manager can approve anything today | Same pattern as #17 — product confirms intended scope, new system enforces it | Before leave/requests module cutover |
| [#19](https://github.com/workin-hr/hr-legacy/issues/19) | Two parallel, non-identical employee self-registration endpoints | Medium | P1 | Engineering | **Resolved by F-01** (`join_company` confirmed canonical) — remaining action is confirming `register_employee` is fully dead before dropping it | `join_company` behavior ported as the single registration flow; `register_employee` confirmed dead across all callers, not just the two Flutter clients | Before auth/onboarding module cutover |
| [#20](https://github.com/workin-hr/hr-legacy/issues/20) | Employee deletion cascade-deletes payroll/financial history, working around the schema's own RESTRICT constraint | Low | P2 | Engineering + Product | Non-blocking | Product confirms whether financial-history retention on deletion is a compliance requirement; new schema encodes whichever answer is given as a real constraint, not an app-level workaround | Before employee-lifecycle module cutover |
| [#21](https://github.com/workin-hr/hr-legacy/issues/21) | Payroll batch creation uniqueness is app-level only — no DB constraint | Low | P2 | Engineering | Non-blocking. **Progress 2026-08-07**: V10 carries a real UNIQUE constraint on the (company, month, year) batch key — a duplicate create attempt is a clean 409, no app-level pre-check race (`PayrollBatchLifecycleTest.duplicateBatchForTheSameCompanyMonthYearIsRejectedNotDuplicated`) | New schema has a real unique constraint (or equivalent) on the batch-period key | Before payroll module cutover |
| [#22](https://github.com/workin-hr/hr-legacy/issues/22) | `payroll_batches/calculate.php` isn't transactional, unlike `finalize`/`reopen` | Low | **Done 2026-08-07** | Engineering | **Done** — `PayrollBatchService.calculate` is a single `@Transactional` method like finalize/reopen, and the row's own closure criterion is now met: `PayrollBatchLifecycleTest.aMidCalculationFailureLeavesThePriorPayslipsExactlyAsTheyWere` injects a real mid-loop failure (a poisoned contract whose computed gross overflows NUMERIC(10,2), failing after the first employee's payslip was already written) and proves full rollback — the pre-existing payslip survives with its original row id, nothing from the aborted run persists | New payroll-calculation code path is transactional, verified by a test that simulates a mid-calculation failure | Closed |
| [#23](https://github.com/workin-hr/hr-legacy/issues/23) | `penalties/report.php`'s "CSV" export is actually XLSX | Low | P3 | Engineering | Non-blocking | New export endpoint's content-type and extension match its actual format | Backlog |
| [#24](https://github.com/workin-hr/hr-legacy/issues/24) | `attendance/stats.php` has mislabeled/placeholder response fields | Low | P3 | Engineering | Non-blocking | New stats endpoint's field names match their actual semantics; no hardcoded placeholder values | Backlog |
| [#25](https://github.com/workin-hr/hr-legacy/issues/25) | Bulk attendance deletion has no dry-run/audit trail | Low | P2 | Engineering + Product | Non-blocking. **Note 2026-08-07**: the attendance first slice deliberately ships no bulk delete-range endpoint — the decision remains open with nothing to retrofit | Product confirms whether a dry-run/confirm step and audit log are required; new endpoint implements whichever is decided | Before attendance module cutover |

## Section B — `hr-platform` Pre-Migration Readiness Gaps (Issues #9–#16)

Full gap text lives in
[`pre-migration-readiness-gap-analysis.md`](./pre-migration-readiness-gap-analysis.md).
This section only restates the tracking fields; it does not duplicate the
Description/Why-It-Matters prose already there.

| # | Title | Severity | Priority | Owner Type | Blocking? | Evidence For Closure | Dependency / Target Milestone |
|---|---|---|---|---|---|---|---|
| [#9](https://github.com/workin-hr/hr-platform/issues/9) | PMR-01: Dashboard Discovery coverage incomplete (~75 of 92 files unread) | Medium | P1 | Engineering | Blocking: full-confidence ADR-0002/ADR-0003 acceptance | Remaining ~75 dashboard files read and cross-referenced against `existing-endpoint-inventory.md` | Before ADR-0002/0003 acceptance |
| [#10](https://github.com/workin-hr/hr-platform/issues/10) | PMR-02: Flutter client contract still needs module-by-module closure | Medium (was Critical/High) | P1 | Engineering | Ready — not a whole-program blocker, but still gates each affected module's cutover | `docs/api/flutter-request-response-compatibility.md` plus module-specific closure for whichever API surface is being cut over | Before each client-facing module's cutover |
| [#11](https://github.com/workin-hr/hr-platform/issues/11) | PMR-03: Production data needs fresh-snapshot re-verification | Medium (was High) | P1 | Manual Operator + Engineering | Ready — substantial analysis exists; fresh evidence is still required near cutover time | Someone with production DB access reruns the documented query set against a fresh snapshot and records the updated evidence | Before data-migration/cutover phase |
| [#12](https://github.com/workin-hr/hr-platform/issues/12) | PMR-04: Attendance device/hardware Discovery not started | High | P1 | Manual Operator + Engineering | Blocking: device-integration phase | Device Discovery doc filled in with real hardware/protocol evidence | Before device integration |
| [#13](https://github.com/workin-hr/hr-platform/issues/13) | PMR-07: Technical spike — **executed and accepted 2026-08-05** | High → Done | P0 → done (narrowed 2026-08-04, executed and accepted 2026-08-05) | Engineering | **Closed** — 6/6 tests passing on a clean rebuild, recommendation recorded (RLS, non-superuser-role condition) and accepted in full by the repository owner (D-018). Three conditions from the ADR now carry forward as implementation acceptance criteria for the first module using RLS | `docs/migration/technical-spike-plan.md` "Full Spike Findings" section; `docs/bootstrap/decision-log.md` D-018 | None remaining — closed |
| [#14](https://github.com/workin-hr/hr-platform/issues/14) | PMR-08: All 10 architecture ADRs remain Proposed | High → Low | **Done 2026-08-05** — 10 of 10 ADRs have a recorded decision | Product + Engineering | **Done 2026-08-05** for all 10 ADRs (`docs/bootstrap/decision-log.md` D-016 through D-026; ADR-0002 fully accepted, ADR-0010 accepted in full, ADR-0006 Part A only — Part B's protocol detail remains its own tracked sub-item, not a separate ADR) | Each ADR's `Status` moved to `Accepted` (or a considered `Rejected`/`Deferred`) by an explicit human decision, recorded in the ADR file itself | None remaining at the ADR level — closed |
| [#15](https://github.com/workin-hr/hr-platform/issues/15) | PMR-09: No detailed per-module migration execution plan beyond the already-authorized backend auth/tenant-identity slice | Medium | P1 | Engineering | Blocking: first broader migration wave/cutover planning, not the D-028-authorized backend start itself | Per-module execution plan doc exists, covering sequencing, rollback, and acceptance criteria per module | Before the first broader migration wave or any module cutover |
| [#16](https://github.com/workin-hr/hr-platform/issues/16) | PMR-10: No migration-correctness test plan or differential-testing harness | High | P0 — **mechanism shipped 2026-08-07** | Engineering | Blocking: data-migration phase. **Shipped**: `docs/migration/migration-correctness-test-plan.md` (check layers, canonical export convention, golden-dataset format/capture procedure, cutover run procedure) and `scripts/migration_diff.py` (stdlib-only reconciliation harness — counts vs recorded baseline, checksums, named key/column diffs — self-tested in CI across every detection class, i.e. it has run against a non-trivial synthetic sample). **Remaining, data- or module-bound**: per-table export SQL (with F-15's conversion scripts), payroll golden-dataset capture (with the payroll module, needs legacy staging), and the production-snapshot run (cutover-time operator action, recorded in `migration-validation-queries.md`) | Differential-testing harness exists and has run at least once against a non-trivial data sample; production-run evidence recorded per `migration-validation-queries.md` | Production-snapshot run: data-migration/cutover phase |

## Section C — New Flutter Discovery Findings (F-01–F-12)

Findings identified during the 2026-08-04 read-only Flutter client
Discovery pass. Full text in
[`flutter-request-response-compatibility.md`](../api/flutter-request-response-compatibility.md)
unless noted. IDs are internal to this matrix (not GitHub issue numbers)
except where a GitHub issue was filed to track the action item.

| ID | Title | Severity | Priority | Owner Type | Blocking? | Evidence For Closure | Dependency / Target Milestone |
|---|---|---|---|---|---|---|---|
| F-01 | `join_company` confirmed canonical registration flow | Low (informational — resolves an ambiguity) | P1 | Engineering | Non-blocking, closes an open question on #19 | Tracked via `hr-legacy#19` comment (2026-08-04) | Before auth/onboarding module cutover |
| F-02 | Plaintext SharedPreferences token storage + no client-side refresh capability | High | P0 | Engineering + Security | Blocking: auth-remediation rollout. **Server half exists 2026-08-06**: the refresh endpoints the clients need (`POST /api/auth/refresh`, `POST /api/auth/logout`) are implemented and tested in the new backend, so the client workstream is no longer blocked on a missing server contract | GitHub issue [`hr-platform#18`](https://github.com/workin-hr/hr-platform/issues/18); closes when `docs/security/authentication-remediation-design.md`'s client-side workstream (secure storage + refresh-capable networking) is implemented and shipped | Before/alongside JWT lifetime change in new backend |
| F-03 | *(merged into F-02 — client storage and refresh capability are one workstream)* | — | — | — | — | — | — |
| F-04 | QR check-in has no confirmed live client caller — real-world exploitability of #16 depends on this | Low (informational) | P2 | Product | Non-blocking until answered | Tracked via `hr-legacy#16` comment (2026-08-04); closes when product confirms whether a third client (kiosk/tablet/dashboard) calls `check_in_qr` | Before attendance module cutover, if a live caller is confirmed |
| F-05 | Desktop client references a server endpoint that was never built (`set_employee_attendance_method`) | Low | P2 | Product | Non-blocking | GitHub issue [`hr-platform#19`](https://github.com/workin-hr/hr-platform/issues/19); closes when product confirms abandoned-vs-planned | Before attendance-config module cutover, if confirmed planned |
| F-06 | No client-side environment/staging switch — both apps hardcode a single production `baseUrl` | Low | P2 | Engineering | Blocking: staged cutover testing, if wanted | GitHub issue [`hr-platform#20`](https://github.com/workin-hr/hr-platform/issues/20); closes when environment configuration is added to the client, if a staged-cutover test plan requires it | Before data-migration/cutover phase, only if staged testing against the new backend is chosen |
| F-07 | Desktop forced-update/maintenance-mode remote-config mechanism must be preserved | Medium | P1 | Engineering | Blocking: desktop-facing module cutover | GitHub issue [`hr-platform#21`](https://github.com/workin-hr/hr-platform/issues/21); closes when the new backend's `configs`-equivalent surface serves the same version-gate fields, verified against a real desktop build | Before any module cutover that desktop clients use |
| F-08 | FCM push notification delivery — **confirmed 2026-08-04: not functionally working end-to-end today** (mobile's token-fetch/registration call is commented out; server's `FCM_SERVER_KEY` is a placeholder) — build for the first time, not preserve | Medium | P1 | Engineering | Blocking: notifications module cutover | GitHub issue [`hr-platform#22`](https://github.com/workin-hr/hr-platform/issues/22); closes when the backend implements real FCM sending **and** the mobile client's stubbed token-fetch/registration call is un-stubbed (a scoped client-code exception, similar in kind to the auth-storage exception) | Before notifications module cutover |
| F-09 | No offline persistence in either client — confirmed absent | Low | P3 | Product | Non-blocking | GitHub issue [`hr-platform#23`](https://github.com/workin-hr/hr-platform/issues/23); closes when product records an explicit decision (in scope for a future release, or not) | Backlog |
| F-10 | Three real frontends exist (PHP dashboard, Flutter desktop, Flutter mobile), not two — dashboard-vs-desktop fate | High (architecture-level) | Done | Product | **Decision recorded** 2026-08-04 (Option E, role-based split); `hr-legacy#26` investigated and resolved non-blocking; Engineering sign-off (Next.js), feature-disposition (`salary_calculator`, etc.), and desktop-access-universality all resolved 2026-08-05 (repository owner confirmed the desktop app ships as a standard `.exe`/`.dmg` installer) | Tracked via `docs/adr/ADR-0009-dashboard-vs-desktop-admin-client.md`; `Status` moved to `Accepted`, `docs/bootstrap/decision-log.md` D-025 | None remaining — closed |
| F-11 | `dsa_priv.pem` (desktop auto-update signing private key) custody unverified from source | Low | P3 | Manual Operator | Non-blocking | Tracked via [`hr-platform#24`](https://github.com/workin-hr/hr-platform/issues/24) (`docs/security/gcp-firebase-credential-verification-checklist.md`, item 8); closes when whoever performs desktop releases confirms secure storage (password manager / CI secret, not a plain file) | Non-blocking, verify opportunistically |
| F-12 | Manager-role mobile "Manager Mode" is an unimplemented UI stub (`profile_manager_mode_button.dart` — "coming soon"); Manager cannot log into desktop by design, not by gap (investigated 2026-08-04, `hr-legacy#26`) | Low (reclassified from Medium — not a migration blocker) | P2 | Product + Engineering | **Non-blocking** for ADR-0009/dashboard retirement (reclassified 2026-08-04 — see `hr-legacy#26` for full evidence: 0 real `manager`-role employees exist in production data, and the mobile client's own "Manager Mode" stub indicates Manager capability was designed for mobile, not desktop) | [`hr-legacy#26`](https://github.com/workin-hr/hr-legacy/issues/26); closes when product decides whether/when to build mobile "Manager Mode" for real | Product backlog — not tied to any migration-readiness gate |
| F-13 | RLS-arm test-coverage gap from the H2 spike: no test yet proves RLS's fail-closed behavior when the tenant session-variable-setting call itself is omitted | Medium | Done | Engineering | **Done 2026-08-05** — real, implemented, passing (`backend/src/test/java/com/workin/backend/tenancy/RlsFailClosedTest.java`, `unscopedReadOfARealRowReturnsNothingWhenNoTenantIsSelected`), part of the first real backend implementation (Phase 1, `docs/bootstrap/decision-log.md` D-028) | Full Given/When/Then spec: `docs/adr/ADR-0002-modular-monolith-baseline.md` (Decision, condition 3); real test: `backend/src/test/java/com/workin/backend/tenancy/RlsFailClosedTest.java`, passing in CI (`.github/workflows/backend-validate.yml`) | None remaining — closed |
| F-14 | ADR-0010 Task 1: map every legacy `hr_permissions` column to a canonical permission key | Low — **done as part of ADR-0010's acceptance 2026-08-05** | Done | Solution Architect | **Done** — full 17-row mapping table produced, correcting the earlier "18... and one more" placeholder count | `docs/architecture/authorization-model.md` §7; closed | None — closed |
| F-15 | ADR-0010 Task 2: migrate legacy roles/permission flags into memberships, roles, permissions, overrides, and resource scopes | Medium | P1 | Engineering | **Blocking** for cutover of any module enforcing fine-grained permissions | **Mechanism live 2026-08-06**: `membership_permission_overrides` (V18, RLS V19), `role_permissions` default bundles (V20 — `COMPANY_ADMIN` full tenant catalog per confirmed legacy behavior; `HR`/`MANAGER`/`EMPLOYEE` deliberately empty), `PermissionEvaluationService` implementing Dimension 3's precedence inside the validated-context transaction, and runtime `@RequiresPermission` enforcement (the freeze rule was removed in the same PR, per its own condition, replaced by a platform-domain confinement rule). **Remaining for closure**: the legacy-data conversion run — mapping every real `hr_permissions` row to ALLOW-override rows per §7's rule against a production snapshot near cutover | Before authorization-enforcing modules go live |
| F-16 | ADR-0010 Task 3: resolve the intended scope of every existing manager before migration (`hr-legacy#17`/`#18`) | Medium | P1 | Product + Engineering | **Blocking** for `MANAGER`-role cutover — legacy's unscoped company-wide access is not carried forward by default | Real investigation, not a mechanical copy of current (drifted) behavior; closes when every `manager`-role employee has explicit `membership_resource_scopes` rows | Before any `MANAGER`-role user is migrated |
| F-17 | ADR-0010 Task 4: permission-matrix test suite covering every role and capability | Medium | P1 — **started 2026-08-06** | Engineering | Blocking for authorization-module completion. `PermissionEvaluationTest` covers the precedence chain (deny-beats-role, allow-beyond-role, deny-by-default) and the shipped role bundles (`COMPANY_ADMIN` full-tenant/no-platform; `HR` empty-by-default); per-capability coverage grows with each business module's endpoints | Real automated test suite; closes when every role/capability combination in `docs/architecture/authorization-model.md` §7 has test coverage | Before authorization module is considered implementation-complete |
| F-18 | ADR-0010 Task 5: negative cross-tenant tests for every sensitive module, modeled on `hr-legacy#2`/`#3`/`#5`/`#6` | Medium | P1 — **first module covered 2026-08-07** | Engineering | Blocking per-module. Covered so far: employees (`EmployeeModuleFlowTest`), advances (`AdvanceModuleFlowTest`, retiring `hr-legacy#5`'s op shapes for the operations that exist), penalties (`PenaltyModuleFlowTest`, including delete), and the payroll group (`PayrollModuleFlowTest`, 2026-08-07 — salary-contracts, payroll-batches, and payslips, including proof that a foreign batch's DRAFT/FINALIZED state never leaks through a 409 where an in-tenant caller would receive one), attendance (`AttendanceModuleFlowTest` + `ExceptionTypeFlowTest`, 2026-08-07 — including cross-tenant exception-type references on create), member administration (`MemberAdminFlowTest`, 2026-08-07 — every role/override/status operation on a foreign membership is the same 404), requests + leave-balances (`RequestModuleFlowTest` + `LeaveBalanceFlowTest`, 2026-08-07 — including approve/reject on foreign requests and foreign request-type/exception-type references on create), and the organization group (`OrganizationStructureFlowTest` + the employees attribution cases, 2026-08-07 — branches/departments/job-titles/shifts cross-tenant probes and foreign manager/branch-set/org-attribution references all the same 404) — cross-tenant reads and mutations are 404s, cross-tenant rows never appear in lists; the test shape is the template each subsequent module copies | Real automated tests; closes per module as it's implemented | Before each sensitive module is considered implementation-complete |
| F-19 | ADR-0010 Task 6: tests proving revoked permissions stop working on the next request | Medium | **Done 2026-08-06** | Engineering | **Done** — `AuthorizationEnforcementFlowTest.anAllowOverrideGrantsAndItsRevocationBitesOnTheVeryNextRequest`: an ALLOW flipped to DENY is rejected on the immediately following request with the same access token — no re-login, refresh, or expiry involved (Dimension 5's core guarantee, structurally ensured by per-request recomputation with no cross-request cache). **2026-08-07**: also proven end-to-end through the real administration surface (`MemberAdminFlowTest` — an override granted via the API takes effect on the target's next request and stops after revocation) | Passing in CI | Closed |
| F-20 | ADR-0010 Task 7: tests for inactive memberships and mismatched identity/membership combinations | Medium | **Done 2026-08-06** | Engineering | **Done** — identity/membership mismatch was already covered (`TenantContextIsolationTest`); the previously missing disabled-membership case is now real (`AuthorizationEnforcementFlowTest.aDisabledMembershipFailsClosedAtTheGate`): a disabled membership fails closed at the authorization gate on the next request with the same token. **2026-08-07**: also proven end-to-end through the real administration surface (`MemberAdminFlowTest` — disable via the API fails the member closed next request; reactivation restores them) | Passing in CI | Closed |
| F-21 | ADR-0010 Task 8 = F-13 (same requirement, cross-referenced, not duplicated work) | — | Done | — | See F-13 | See F-13 | Closed |
| F-22 | ADR-0010 Task 9: startup check that fails when the runtime PostgreSQL connection is a superuser or bypasses RLS | Medium | Done | Engineering | **Done 2026-08-05** — real, implemented, passing (`backend/src/main/java/com/workin/backend/config/SuperuserStartupCheck.java`, tested by `SuperuserStartupCheckTest`: fails on the container's superuser credentials, passes on `app_runtime`) | `backend/src/main/java/com/workin/backend/config/SuperuserStartupCheck.java`; `backend/src/test/java/com/workin/backend/config/SuperuserStartupCheckTest.java`, passing in CI | None remaining — closed |
| F-23 | ADR-0010 Task 10: architecture test detecting application use cases without explicit authorization-policy declarations | Medium | **Done 2026-08-06** | Engineering | **Done** — `backend/.../authorization/`: `@PublicUseCase`/`@AuthenticatedUseCase`/`@RequiresPermission` declarations, typed `PermissionKeys` kept in exact sync with V4's catalog (`PermissionCatalogSyncTest`), and `AuthorizationPolicyArchTest`'s three build-failing rules — every handler declares exactly one policy; policies only on handlers; and `@RequiresPermission` is **frozen** (any usage fails the build) until F-15/F-17 land runtime enforcement, so no decorative permission gate can ship. Proven-to-fail fixture tests keep the closure criterion alive in the suite. Follow-up (not a reopening): extend the completeness rule to the application-service layer when the first business module introduces it | `AuthorizationPolicyArchTest`, `PermissionCatalogSyncTest`, all passing in CI; all nine existing handlers annotated | Closed; the service-layer rule extension lands with the first business module |
| F-24 | ADR-0010 Task 11: privilege-escalation tests for role and permission administration | Medium | **Done 2026-08-07** | Engineering | **Done** — the administration surface itself now exists (`com.workin.backend.members`: role assign/remove, ALLOW/DENY override grant/revoke, membership activate/disable, gated by the new `members.read`/`members.manage` keys, every mutation audited in `tenant_audit_events` with actor/target attribution) and `MemberAdminFlowTest` covers §8's rules with real endpoints: self-role/override/status change → 409; last-admin removal **and** last-admin disable → 409; `platform.*`/unknown-key grants → uniform 404; plus end-to-end proof that grants/revocations and disabling bite on the target's next request | Real automated tests covering §8's rules (self-role-change, last-admin removal, platform/tenant namespace crossing); passing in CI | Closed |
| F-25 | ADR-0010 Task 12: manager-scope tests for branch, department, direct-report, and specifically-assigned-employee scope assignments | Medium | P1 | Engineering | Blocking for `MANAGER`-role module completion | Real automated tests covering every `membership_resource_scopes` type; closes when they exist and pass in CI | Before `MANAGER`-role modules are considered implementation-complete |
| F-26 | Independent per-platform-admin account identity (individual credentials, sessions, revocation, audit attribution) — replacing the shared platform-admin password (`hr-legacy#11`) | High (real, confirmed security/audit gap; not accepted as MVP architecture) | **P0** | Product + Engineering | **Substantially closed 2026-08-06** in two increments: (1) individual identity, credentials, login, and structurally-separated JWT sessions; (2) individual session revocation (rotating opaque refresh tokens in `platform_admin_refresh_tokens`, per-session logout, reuse-detection family revocation, revoke-all primitive, fail-closed rotation for deactivated admins) and audit attribution (`platform_admin_audit_events` with a NOT NULL admin FK; `PlatformAdminAuditService` as the single write path; `LOGIN`/`LOGIN_FAILED`/`LOGOUT`/`SESSION_REUSE_REVOKED`/`ALL_SESSIONS_REVOKED` recorded). **Remaining before `platform.*` business functionality ships**: every such endpoint must record its action through `PlatformAdminAuditService` — a standing acceptance criterion (ArchUnit-enforceable alongside F-23), not an unimplemented subsystem | `hr-legacy#11`; `backend/src/main/java/com/workin/backend/platformadmin/`; tests `PlatformAdminBootstrapTest`, `PlatformAdminAuthFlowTest`, `PlatformAdminSessionFlowTest`, `PlatformAdminAuditTest`, `PlatformAdminDomainSeparationTest` | Per-endpoint audit recording: as each `platform.*` endpoint is built, before it reaches production |
| F-27 | No minimum password length/complexity on `register_company.php`/`reset_password.php` (zero check beyond non-empty); `change_password.php` only enforces 6 characters, no complexity rule (`hr-legacy#27`) | Medium | P2 | Engineering | **Non-blocking today** — the new backend's company self-registration already carries the fix (`backend/src/main/java/com/workin/backend/identity/RegisterCompanyRequest.java`, `@Size(min=8, max=128)`, 2026-08-06). Blocks only the reset-password/change-password endpoints specifically, which don't exist in the rewrite yet | GitHub issue [`hr-legacy#27`](https://github.com/workin-hr/hr-legacy/issues/27); closes when the new backend's reset-password and change-password endpoints enforce the same minimum-length rule already applied to registration, rather than repeating the legacy inconsistency | Before reset-password/change-password endpoints are implemented in the new backend |

## Summary Counts

Manual aggregate counts were removed on Wednesday, August 5, 2026
because they had drifted away from the table above and were misleading.
Use the per-row table as the source of truth; if aggregate counts are
needed later, they should be regenerated from the current table rather
than maintained by hand.

## Evidence

Section A: `hr-legacy` issues #2–#25, `docs/security/threat-model.md`,
`docs/legacy/business-rule-extraction.md`. Section B:
`docs/migration/pre-migration-readiness-gap-analysis.md`, `hr-platform`
issues #9–#16. Section C:
`docs/api/flutter-request-response-compatibility.md`,
`docs/security/pre-migration-flutter-credential-inventory.md`,
`docs/adr/ADR-0009-dashboard-vs-desktop-admin-client.md`, `hr-platform`
issues #18–#25, `hr-legacy` issues #26–#27 and issue comments on #16
and #19 (2026-08-04).
