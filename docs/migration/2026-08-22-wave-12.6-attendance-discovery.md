# Wave 12.6 Discovery — Attendance and Schedules

## Status and evidence boundary

Discovery only. **No Java is written and none starts** until the owner decisions
in §J are settled — the gate D-073 set for Wave 12.4 and Wave 12.5 followed.

Evidence is `hr-legacy` source and `mysql_workin.schema.sql` under D-058's
precedence: production > PHP > schema (including `ALTER TABLE`) > docs >
existing Java. **Existing Java/PostgreSQL code was not used as a specification**
at any point — the `com.workin.backend.attendance`/`schedule`/`payroll` modules
are a different design and are treated as prior art, not authority.

**No production access is available. J1–J4 were not run.** Every claim below is
static evidence with a file and line that can be re-read. Nothing here proposes
a schema, data, dependency, Gradle, decision-log or production-Java change.

Base: `0f40758`, the closed Wave 12.5 tree.

## A. The plan's module list is wrong — corrected inventory

`docs/migration/2026-08-18-item-12-specification.md` assigns Wave 12.6
"`attendance` (+`exception_types` consumers), `employee_schedules`,
`employee_shift_assignments`". Derived from source, that is not the surface:

| Plan says | Source says |
|---|---|
| `attendance` | **15 endpoints** in `apis/api/attendance/` |
| `employee_schedules` | the module is `apis/api/schedules/` — **3 endpoints**, and it is not named for the table |
| `employee_shift_assignments` | **no endpoint directory exists** |

`employee_shift_assignments` has **no endpoints of its own**. Every write to it
happens inside `employees/create.php` and `employees/update.php` — both
**already ported in Wave 12.4** — and it is read by `attendance/list.php`,
`employees/list.php`, and five helpers. This is exactly the `department_branches`
shape D-054 found in Wave 12.3: a table that is part of another module's
transaction, not a module.

**Wave 12.6 is 18 endpoints across 2 modules.**

### A.1 Complete endpoint inventory

| # | Route | Method | Lines |
|---|---|---|---|
| 1 | `/apis/api/attendance/list.php` | GET | 288 |
| 2 | `/apis/api/attendance/one.php` | GET | 30 |
| 3 | `/apis/api/attendance/create.php` | POST | 119 |
| 4 | `/apis/api/attendance/update.php` | PUT | 113 |
| 5 | `/apis/api/attendance/delete.php` | DELETE | 50 |
| 6 | `/apis/api/attendance/delete_range.php` | DELETE | 67 |
| 7 | `/apis/api/attendance/check_in.php` | POST | 63 |
| 8 | `/apis/api/attendance/check_in_qr.php` | POST | 76 |
| 9 | `/apis/api/attendance/check_out.php` | POST | 61 |
| 10 | `/apis/api/attendance/stats.php` | GET | 102 |
| 11 | `/apis/api/attendance/employee_monthly_attendance.php` | GET | 121 |
| 12 | `/apis/api/attendance/overall_report.php` | GET | 38 |
| 13 | `/apis/api/attendance/export.php` | GET | 50 |
| 14 | `/apis/api/attendance/analyze_excel.php` | POST | 46 |
| 15 | `/apis/api/attendance/import_excel.php` | POST | 106 |
| 16 | `/apis/api/schedules/assign_employee_schedule.php` | POST | 75 |
| 17 | `/apis/api/schedules/employee_monthly_schedule.php` | GET | 44 |
| 18 | `/apis/api/schedules/generate_employee_schedule.php` | POST | 45 |

**1,494 lines of endpoint code**, before helpers. `attendance/list.php` alone is
larger than any single Wave 12.5 endpoint.

## B. Authority and permission matrix

| Endpoints | `requireAuth` argument | Permission flag |
|---|---|---|
| 13 of 15 attendance endpoints | **bare `requireAuth()`** — every authenticated role | **none** |
| `employee_monthly_attendance` | CA, HR, MANAGER, EMPLOYEE | none |
| `overall_report` | CA, HR, MANAGER | none |
| `schedules/assign_employee_schedule`, `generate_employee_schedule` | CA, HR, MANAGER | none |
| `schedules/employee_monthly_schedule` | CA, HR, MANAGER, EMPLOYEE | none |

**Not one endpoint in this wave gates on an `hr_permissions` flag.** That is
consistent with D-076's measured enforcement map — only `can_company_settings`
and `can_employees` are ever enforced, at 22 sites, none of them here — so it is
reproduced rather than treated as an omission. See §I.1 for why the bare
`requireAuth()` on the write paths is nonetheless a security question.

## C. Tenancy — no table in this wave has a `company_id`

```text
attendance                  (schema:121-132)   employee_id -> employees
employee_schedules          (schema:455-464)   employee_id -> employees
employee_shift_assignments  (schema:476-484)   employee_id -> employees, shift_id -> shifts
```

All three are **employee-derived tenancy** (P-1b), the policy Wave 12.2 built and
Wave 12.4 first exercised at scale. There is no direct-company predicate
available anywhere in the wave; every endpoint must reach `employees.company_id`
through a join or a prior lookup. `attendance/list.php` does this with
`$company_id` bound into a subquery at lines 43, 133 and 277.

## D. Schema, FK and index matrix

| Table | Unique | Indexes | FK behaviour |
|---|---|---|---|
| `attendance` | **none** | `fk_attendance_employee`, `index_employee_date (employee_id, check_in)`, `fk_attendance_exception_type` | `employee_id` → `employees` **ON DELETE CASCADE**; `exception_type_id` → `exception_types` **ON DELETE SET NULL** |
| `employee_schedules` | **`uniq_employee_schedule_date (employee_id, schedule_date)`** | `fk_employee_schedule_employee` | `employee_id` → `employees` **ON DELETE CASCADE** |
| `employee_shift_assignments` | **none** | `idx_esa_employee`, `idx_esa_shift`, `idx_esa_employee_effective (employee_id, effective_from)` | `employee_id` → `employees` **CASCADE**; `shift_id` → `shifts` **CASCADE** |

Three consequences worth stating now:

1. **`attendance` has no unique constraint at all.** Duplicate check-ins are
   prevented only by application logic (§F), so any gap in that logic writes a
   real duplicate row. Contrast Wave 12.5's holidays, where the database was the
   backstop.
2. **`employee_schedules` enforces one row per employee per day**, so
   `generate_employee_schedule` must be idempotent or handle the collision — a
   detail to confirm per endpoint during implementation.
3. **Deleting a shift cascades into `employee_shift_assignments`.** Wave 12.5's
   `shifts/delete.php` is a *soft* delete, so this cascade is not reachable
   through that endpoint — but it is reachable through
   `company_delete_helper.php`, and it means a hard shift delete silently
   unassigns employees.

## E. D-083 audit — the largest exposure in Phase 1 so far

**11 of 18 endpoints** contain timezone-sensitive constructs:

| Endpoint | Constructs |
|---|---|
| `check_in` | `NOW()`, `TIMESTAMPDIFF`, `date()` |
| `check_in_qr` | `NOW()`, `date()` |
| `check_out` | `NOW()`, `date()`, `strtotime` |
| `create` | `TIMESTAMPDIFF`, `date()`, `strtotime` |
| `delete_range` | `DATE()`, `date()`, `strtotime` |
| `employee_monthly_attendance` | `TIMESTAMPDIFF`, `date()`, `strtotime` |
| `list` | `DATE()`, `TIMESTAMPDIFF`, `date()`, `new DateTime`, `strtotime` |
| `overall_report` | `date()` |
| `stats` | `DATE()`, `TIMESTAMPDIFF`, `date()`, `strtotime` |
| `update` | `date()`, `strtotime` |
| `employee_monthly_schedule` | `date()` |

This is qualitatively worse than earlier waves, for one reason:

**`check_in.php:56` writes the attendance timestamp with `NOW()`** — the
database decides when the employee checked in — while **`check_in.php:63`
reports `date('Y-m-d H:i:s')`**, the application clock. A single response
therefore mixes two clocks: the row's `check_in` comes from the database session
timezone, and the `time` field the client is shown comes from `LegacyClock`.
Until D-083 is closed those two disagree by the offset difference, in a value a
client displays to the employee.

The 2-hour duplicate guard (`check_in.php:35`) is also evaluated with `NOW()`,
so the window itself moves with the session timezone.

**D-083 is not closed by this wave and no `SET time_zone` is proposed.** It is
recorded here as materially more urgent than it was: earlier waves exposed
timezone-sensitive *reads*; this wave has timezone-sensitive **writes** whose
values become permanent rows.

## F. Check-in / check-out / QR map

### F.1 `check_in.php` — statement order

```text
method guard -> requireAuth() (any role) -> requireCompanyActive
-> body(); required(latitude, longitude, method)
-> employee_id = body.employee_id ?? auth.employee_id ?? 0   <-- see I.1
-> employee lookup scoped by id + company_id  (404 invalid_employee)
-> is_self_attendance = auth.type === 'employee' && auth.employee_id === employee_id
-> if (is_self_attendance && empty(is_mobile_attendance_enabled)) 403
-> attendance_find_open_session(employee_id)  -> already_checked_in
-> last check-in; TIMESTAMPDIFF(MINUTE, last, NOW()) < 120 -> 422
-> if (!empty(lat) && !empty(lng)) validate_employee_attendance_location(...)
-> INSERT ... VALUES (?, NOW(), ?, ?, ?)
-> ok(check_in_recorded, {attendance_id, time: date('Y-m-d H:i:s')})
```

No transaction anywhere. The open-session probe, the 2-hour probe and the INSERT
are three independent statements, so two concurrent requests can both pass the
probes and both insert — and there is no unique constraint to stop them (§D.1).

### F.2 Behaviours that must be ported literally

- **`required()` passes `"0"`, `!empty()` does not.** `latitude` and `longitude`
  are `required`, then geofence validation is guarded by
  `!empty($lat) && !empty($lng)`. A check-in at latitude `0` or longitude `0`
  therefore passes `required()` and **skips location validation entirely**. See
  §I.2.
- **The 422 carries a hard-coded Arabic string**, not a lang key:
  `'لا يمكن تسجيل بصمتين متتاليتين خلال اقل من ساعتين'` (`check_in.php:38`). It
  is returned as `Response::REASON` and is not translated for an English client.
- `method` is in `required()`, so the `?? AttendanceMethodEnum::APP` fallback on
  the INSERT is **dead code**. Reproduce the fallback anyway or drop it — it
  cannot change behaviour either way, and this document flags it so a reviewer
  does not read its absence as a defect.

### F.3 `check_in_qr.php`

QR is validated against **`branches.qr_code` plus an expiry**, scoped by
`company_id` (`check_in_qr.php:23-38`); a miss is `invalid_qr`. The matched
branch id becomes the attendance branch context. The insert records
`method = 'qr'`. No token, nonce or signature is involved — the QR value is a
stored column, so it is a **bearer secret with no replay protection** beyond its
expiry. Recorded as an observation, not a proposed change.

### F.4 `attendance_find_open_session()` — a hidden write, fully traced

This was previously deferred to implementation time. That was wrong: it mutates
rows, so it is part of the endpoint contract. Traced in full
(`apis/helpers/attendance_session_helper.php`):

```text
attendance_find_open_session(employee_id, full_row = false)          :170
  1. employee_id <= 0                     -> null, no query
  2. company_id = attendance_employee_company_id(employee_id)        :191
        SELECT company_id FROM employees WHERE id = ?
  3. if company_id > 0:
        attendance_auto_close_stale_open_sessions(company_id, employee_id)   <-- WRITE
  4. SELECT id, check_in, latitude, longitude, exception_type_id
       FROM attendance
      WHERE employee_id = ? AND check_out IS NULL AND check_in IS NOT NULL
      ORDER BY id DESC LIMIT 1
  5. blank check_in                       -> null
  6. attendance_is_exception_only_row(...) -> null   (synthetic rows stay open)
  7. company_id <= 0                      -> return row
  8. deadline = attendance_open_session_deadline(company_id, employee_id, check_in)
     if (new DateTimeImmutable('now') >= deadline):
        attendance_auto_close_stale_open_sessions(...) AGAIN               <-- WRITE
        -> null
  9. return row
```

`attendance_auto_close_stale_open_sessions()` (`:80`) is the write:

```text
  SELECT id, check_in, exception_type_id FROM attendance
   WHERE employee_id = ? AND check_out IS NULL AND check_in IS NOT NULL
   ORDER BY id ASC
  foreach row:
     blank check_in                    -> continue
     attendance_is_exception_only_row  -> continue
     deadline = attendance_open_session_deadline(...)
     asOf < deadline                   -> continue
     checkInTs = strtotime(check_in)
     dateYmd   = date('Y-m-d', checkInTs)
     expected  = attendance_import_expected_for_day(company_id, employee_id, dateYmd)
     worked    = max(0, expected_minutes - ATTENDANCE_INCOMPLETE_PUNCH_DEDUCTION_MINUTES)
     checkOutAt = date('Y-m-d H:i:s', checkInTs + worked * 60)
     UPDATE attendance SET check_out = ?
      WHERE id = ? AND check_out IS NULL                                   <-- WRITE
```

#### What this means for the contract

- **`check_in.php`, `check_in_qr.php` and `check_out.php` can all mutate older
  attendance rows before performing their own operation**, because each calls
  `attendance_find_open_session()` before its main write.
  `employee_monthly_attendance.php` does too, on a **GET**.
- The synthetic `check_out` is **computed, not observed**: it is
  `check_in + (expected_minutes - deduction)`, so it fabricates a plausible
  departure time rather than recording one. Those rows are indistinguishable
  from real check-outs in the schema.
- The write loop is **unbounded** — every stale open row for that employee is
  closed, not just the latest — and there is **no transaction**. A failure
  part-way leaves some rows closed.
- The `UPDATE ... AND check_out IS NULL` guard is the only concurrency
  protection, and it is per-row.
- **Clock semantics are mixed inside one function.**
  `new DateTimeImmutable('now')` and `date()` are the *application* clock
  (`LegacyClock`, +02/+03); the `check_in` values they are compared against were
  written by the *database* with `NOW()`. D-083 therefore does not merely shift
  displayed values here — it decides **whether a row gets auto-closed at all**,
  and what synthetic `check_out` is stored.
- `attendance_open_session_deadline()` (`:40`) walks **up to 8 days forward**
  looking for the next non-rest day, calling
  `attendance_import_expected_for_day()` once per candidate day — so a single
  `find_open_session` can issue many queries and reaches the frozen helpers
  described in §G.

**A GET that writes.** `employee_monthly_attendance.php` is a read endpoint whose
first act can be to close attendance rows. Any Java port that treats GETs as
read-only — a read-only transaction, a replica route, a caching layer — breaks
this. Recorded here so it cannot be discovered late.

## G. Helper dependency closure

Directly required by the 18 endpoints:

```text
attendance_calendar_helper.php
attendance_excel_analyzer.php
overall_attendance_report_helper.php
schedule_helper.php
data_export_helper.php
configs_helper.php
exception_types_helper.php      (Wave 12.1, already ported)
```

Transitively:

```text
attendance_calendar_helper   -> schedule_helper, shift_times (12.5, ported),
                                attendance_excel_analyzer, attendance_session_helper,
                                weekly_rest_credit_helper, official_holidays_helper
attendance_excel_analyzer    -> xlsx_parser, schedule_helper
overall_attendance_report    -> attendance_calendar_helper, official_holidays_helper,
                                payroll_calculation, weekly_rest_credit_helper
schedule_helper              -> payroll_calculation, official_holidays_helper
data_export_helper           -> i18n, payroll_calculation, xlsx_writer,
                                attendance_calendar_helper, overall_attendance_report_helper
```

Two of those cross frozen boundaries and are the subject of §J.

### G.1 Per-endpoint frozen-dependency matrix

Computed from a **function-level** call graph over `apis/helpers/*.php` — every
helper function parsed by brace matching, calls attributed to the function that
makes them, closed transitively from each endpoint's own call sites. A first
attempt at *file* granularity produced false positives (`functions.php` is the
global bootstrap and merely contains the marker strings, so every endpoint
appeared to reach `company_settings`); those results are discarded and not
reported here.

| # | Endpoint | fns | `schedule_helper` | `company_settings` | `payroll_calculation` | holiday helpers | auto-close **write** | Blocking |
|---|---|---|---|---|---|---|---|---|
| 1 | `list` | 50 | yes (4) | **yes** | yes (1) | 1 | no | J.1, J.2 |
| 2 | `one` | 17 | no | no | no | 0 | no | — |
| 3 | `create` | 25 | no | no | no | 0 | no | — |
| 4 | `update` | 21 | no | no | no | 0 | no | — |
| 5 | `delete` | 16 | no | no | no | 0 | no | — |
| 6 | `delete_range` | 17 | no | no | no | 0 | no | — |
| 7 | `check_in` | 47 | yes (4) | **yes** | yes (1) | 1 | **YES** | J.1, J.2, J.3, J.4 |
| 8 | `check_in_qr` | 42 | yes (4) | **yes** | yes (1) | 1 | **YES** | J.1, J.2, J.3 |
| 9 | `check_out` | 46 | yes (4) | **yes** | yes (1) | 1 | **YES** | J.1, J.2, J.3 |
| 10 | `stats` | 45 | yes (4) | **yes** | yes (1) | 2 | no | J.1, J.2 |
| 11 | `employee_monthly_attendance` | 52 | yes (4) | **yes** | yes (1) | 1 | **YES** | J.1, J.2 |
| 12 | `overall_report` | 67 | yes (4) | **yes** | **yes (7)** | **5** | no | J.1, J.2 |
| 13 | `export` | 90 | yes (4) | **yes** | **yes (7)** | **5** | no | J.1, J.2 |
| 14 | `analyze_excel` | 54 | yes (4) | **yes** | yes (1) | 1 | no | J.1, J.2 |
| 15 | `import_excel` | 54 | **no** | **no** | **no** | 0 | no | — |
| 16 | `schedules/assign_employee_schedule` | 27 | yes (3) | **no** | no | 0 | no | — |
| 17 | `schedules/employee_monthly_schedule` | 33 | yes (13) | **yes** | yes (1) | 1 | no | J.1, J.2 |
| 18 | `schedules/generate_employee_schedule` | 30 | yes (7) | **yes** | yes (1) | 1 | no | J.1, J.2 |

**Eleven of eighteen endpoints are blocked on J.1 and J.2**, leaving seven ready.
The previous §K
claim that slices 1–3 and 5 were independent of them was wrong and is withdrawn.

Two results are counter-intuitive and were verified individually rather than
trusted:

- **`import_excel` is clean; `analyze_excel` is blocked.** They look like a pair,
  but `import_excel` calls `attendance_excel_import_punch_log()`, whose closure
  never reaches `attendance_import_expected_for_day()`, while `analyze_excel`
  calls `attendance_excel_analyze()`, whose closure does — and that function is
  the gateway to `schedule_exception_for_day` → `schedule_is_weekly_rest_day` →
  `company_setting_selected_values`.
- **`assign_employee_schedule` is clean** despite reaching `schedule_helper`: it
  uses three write-side schedule functions and none of the weekly-rest ones. It
  does carry its own hidden writes — `schedule_upsert_employee_day` and
  `notification_insert`.

The single chain that blocks the attendance core:

```text
check_in / check_in_qr / check_out / employee_monthly_attendance
  -> attendance_find_open_session
     -> attendance_auto_close_stale_open_sessions        (WRITE)
     -> attendance_open_session_deadline
        -> attendance_import_expected_for_day            (attendance_excel_analyzer)
           -> official_holidays_by_date_in_range
           -> schedule_shift_for_employee_on_date
           -> schedule_exception_for_day
              -> schedule_is_weekly_rest_day
                 -> company_setting_selected_values(WEEKLY_OFF_DAYS)   [Item 13]
                 -> payroll_is_weekly_rest_day                          [12.8/12.9]
```

### G.2 Transitive `payroll_*` closure

Seven payroll functions are reachable from Wave 12.6's helpers:

| Function | Kind | Reads | Settings | Holiday helpers | Writes | Reached by |
|---|---|---|---|---|---|---|
| `payroll_is_weekly_rest_day` | **pure computation** | none | none | none | none | every blocked endpoint |
| `payroll_calculation_as_of_date` | pure (date arithmetic) | none | none | none | none | `overall_report`, `export` |
| `payroll_period_in_progress` | pure (date arithmetic) | none | none | none | none | `overall_report`, `export` |
| `payroll_approved_leave_days` | **DB-backed** | `requests`, `request_types` | none | none | none | `overall_report`, `export` |
| `payroll_employee_work_hours_per_day` | **DB-backed** | `employees`, `job_titles`, `requests`, `request_types` | none | none | none | `overall_report`, `export` |
| `payroll_period_working_days` | **DB-backed** | via holidays | **yes** | `working_days_in_range` | none | `overall_report`, `export` |
| `payroll_expected_work_days_until` | **DB-backed** | via holidays | **yes** | `working_days_in_range` | none | `overall_report`, `export` |

`payroll_enrich_payslip_rows` appears in `data_export_helper`'s file but is not
reached by any Wave 12.6 endpoint's call closure; it reads `attendance`,
`employees` and payslip rows and stays with Waves 12.8/12.9.

Two observations that matter for J.2:

- Only **`payroll_is_weekly_rest_day`** is needed by the nine
  blocked-but-not-reporting endpoints, and it is **pure** — no database, no
  settings, no holidays. That is exactly the narrow extraction the owner
  authorized.
- The other six are needed **only** by `overall_report` and `export`, and two of
  them (`payroll_period_working_days`, `payroll_expected_work_days_until`) drag
  in both `company_settings` **and** the holiday helper D-090 excluded. Those two
  endpoints also read `requests`/`request_types` — **Wave 12.7's table**
  (`requests`) as well as Wave 12.5's.

### G.3 D-090 recomputed by transitive closure

The earlier classification used immediate callers and was wrong for one helper.
Recomputed:

| Helper | Needed by 12.6? | Why |
|---|---|---|
| `official_holidays_by_date_in_range` | **yes** | `attendance_import_expected_for_day` — reached by 11 endpoints |
| `official_holidays_count_in_range` | **yes** | `attendance/stats.php` directly |
| `official_holidays_credit_days_for_employee` | **yes** | `overall_attendance_report_helper` |
| `official_holidays_working_days_in_range` | **yes, if `overall_report`/`export` stay in 12.6** | `payroll_expected_work_days_until` → this. Previously classified payroll-only; that was an immediate-caller error. **Reads `company_settings`.** |
| `official_holidays_is_weekly_rest_day` | **yes, transitively** | called inside `working_days_in_range` |
| `official_holiday_dates_in_range` | **yes, transitively** | called inside `working_days_in_range` |
| `official_holidays_working_credit_details_for_employee` | no | `payroll_calculation` only, not in any 12.6 closure |
| `official_holidays_working_credit_for_employee` | no | as above |
| `official_holidays_count_in_month` | no | **no caller anywhere** — dead code |

**Six of nine** become Wave 12.6's if `overall_report` and `export` stay in
scope; **three of nine** if they are deferred. Three remain excluded either way.

## H. The `company_settings` boundary is crossed — by `schedule_helper`

`schedule_helper.php` calls
`company_setting_selected_values($company_id, CompanySettingEnum::WEEKLY_OFF_DAYS->value)`
at two sites: `:65` inside `schedule_is_weekly_rest_day()`, and `:268` inside
`schedule_company_weekly_rest_dows()`.

Per §G.1 this reaches **11 endpoints**, including the entire check-in/out core —
not only the three `schedules/*` endpoints as first reported. A second settings
path exists through `official_holidays_working_days_in_range`, used by
`overall_report` and `export`.

## I. Dirty-data, tenancy and security findings

### I.1 Attendance on behalf of another employee — corrected

`check_in.php:18` takes the target from `body.employee_id ?? auth.employee_id`
under a bare `requireAuth()`. The employee is looked up by id **and company**, so
this is not cross-tenant; within a company it is a privilege gap, and the three
endpoints do **not** share one mechanism:

- **`check_in.php`** computes
  `is_self_attendance = auth.type === 'employee' && auth.employee_id === employee_id`.
  Targeting a colleague makes it **false**, which *skips* the
  `is_mobile_attendance_enabled` check at `:26` and passes
  `$is_self_attendance = false` into
  `validate_employee_attendance_location()`, relaxing the geofence rule for that
  call.
- **`check_out.php`** uses the same `is_self_attendance` mechanism.
- **`check_in_qr.php` accepts the supplied target too, but has no
  `is_self_attendance` variable and no mobile-attendance check at all.** There is
  nothing for acting-on-behalf to bypass there, because the restriction does not
  exist on that endpoint in the first place. The earlier wording lumped all three
  together and was inaccurate.

### I.2 Latitude or longitude of exactly zero disables geofencing

`required()` accepts `"0"`; `!empty("0")` is false. A caller sending
`latitude: 0` (or `"0"`, or `0.0`) satisfies the required-field contract and then
skips `validate_employee_attendance_location` entirely. Null Island is a real
coordinate.

### I.3 `attendance` has no unique constraint

Two concurrent check-ins can both pass the open-session and 2-hour probes and
both insert — no transaction, no constraint. Legacy has the same race.

### I.4 Spreadsheet surface shares nothing with Wave 12.4

`attendance_excel_analyzer.php` reaches `xlsx_parser.php` and `schedule_helper`,
and does **not** use the employee spreadsheet helper. Wave 12.4's
`LegacyEmployeeSpreadsheet*` stack, its 28-column metadata and its D-085 reader
corrections are **not** transferable on the strength of matching filenames. Only
the format primitives are reuse candidates, each justified against
`xlsx_parser.php`.

### I.5 Notification side effects

`attendance/import_excel.php` and `schedules/assign_employee_schedule.php` only.
Both land on the D-082 boundary; hr-platform#22 still owns delivery.

## J. Owner dispositions — recorded

### J.1 Bounded `company_settings` reader — **option (a), accepted**

A narrowly-scoped, internal, **read-only** compatibility reader for
`company_setting_selected_values(companyId, WEEKLY_OFF_DAYS)`, only as far as
Wave 12.6 requires. No settings endpoints, no writes, no settings
administration, no unrelated keys, no full item 13 port. Recorded as
**D-091**.

Before implementation the authoritative PHP helper and the MariaDB schema behind
it must be documented and its exact query, ordering, default and dirty-data
semantics reproduced. **No weekly-off default may be invented.** That evidence is
not in this document yet and is a precondition of the slice that needs it.

### J.2 `payroll_calculation` — **partially resolved**

`payroll_is_weekly_rest_day(dayOfWeek, restValues)` may be extracted as the pure
compatibility calculation `schedule_helper` needs. §G.2 confirms it is pure: no
database, no settings, no holiday helpers, no writes. This does **not** authorize
moving payroll endpoints, payroll writes, batches or payslips forward.

The broader subset — the six DB-backed functions `overall_report` and `export`
need, two of which reach `company_settings` and the holiday helper D-090
excluded, and which additionally read Wave 12.7's `requests` table — **remains
blocked**. §G.2 is the enumeration that was required; the final J.2 decision is
deliberately **not** recorded yet.

### J.3 Attendance on behalf of — **narrow, accepted**

An authenticated **EMPLOYEE** may target only their own employee id on
`check_in.php`, `check_in_qr.php` and `check_out.php`. COMPANY_ADMIN, HR and
MANAGER keep legacy's same-company target behaviour.

A fail-closed **403** is inserted for an EMPLOYEE supplying a different positive
employee id, **before** any target-specific employee, session, location or branch
information is exposed — and **after** each endpoint's existing method, auth,
company-active and required-body guards, which are preserved in place. Exact
per-endpoint guard order must be documented before implementation. Recorded as
**D-092**.

### J.4 Zero-coordinate geofence bypass — **closed**

Once latitude and longitude have satisfied the endpoint's required-field
contract, geofence validation must not be skipped because either value is
PHP-empty or zero. Zero is a coordinate, not the absence of one. The existing
Phase-1/PHP coercion rules are preserved separately; only truthiness is removed
as the decision to invoke validation. Edge cases must be specified before
implementation. Recorded as **D-093**.

### J.5 The hard-coded Arabic 422 reason — **preserved literally**

`check_in.php:38` returns an untranslated Arabic string as `Response::REASON`.
Reproduced byte-for-byte. No localization key, and **no decision number** — this
is ADR-0011's default, not a divergence.

### J.6 Scope — **approved**

`attendance` 15 + `schedules` 3 = **18**. `employee_shift_assignments` is a table
used by other modules, not an endpoint module. The Item 12 specification needs
the same source-derived correction D-054 applied to `department_branches`; that
edit is **not** made here, because this document is discovery and the
specification is a separate planning artifact.

## K. Implementation slices — revised against the matrix

The first slice plan is withdrawn. Rebuilt from §G.1, and then split further so
that materially different implementation surfaces are not reviewed together:

| Slice | Endpoints | Blocked on | Ready |
|---|---|---|---|
| **12.6.1a** | `attendance/one`, `create`, `update`, `delete`, `delete_range` | nothing | **now** |
| **12.6.1b** | `attendance/import_excel` | nothing | after 1a review |
| **12.6.2** | `schedules/assign_employee_schedule` | nothing | after 1b review |
| **12.6.3** | `check_in`, `check_in_qr`, `check_out` | D-091 evidence, narrow `payroll_is_weekly_rest_day`, **D-092 guard order**, **D-093 edge-case matrix** | not yet |
| **12.6.4** | `list`, `stats`, `employee_monthly_attendance`, `analyze_excel` | D-091 evidence, narrow payroll extraction | not yet |
| **12.6.5** | `schedules/employee_monthly_schedule`, `generate_employee_schedule` | D-091 evidence, narrow payroll extraction | not yet |
| **12.6.6** | `overall_report`, `export` | **broad J.2 (unresolved)** + Wave 12.7's `requests` | not schedulable |

```text
ready now                                    7   (12.6.1a + 1b + 2)
unblocked by D-091 + narrow payroll          9   (12.6.3 + 4 + 5)
unschedulable (broad J.2 + Wave 12.7)        2   (12.6.6)
                                            --
total                                       18
```

**Why `import_excel` is its own slice.** It is dependency-clean — §G.1 proves
that, and it is not blocked. It is separated because its implementation surface
shares almost nothing with the CRUD five: a multipart upload, an independent
attendance spreadsheet parser (§I.4), explicit PDO transaction semantics, its own
error-to-wire mapping, and a D-082 notification side effect. Reviewing it
alongside five small CRUD endpoints would bury it.

**Why `assign_employee_schedule` is its own slice.** Also clean, also different:
it performs repeated schedule upserts (`schedule_upsert_employee_day`) against
`uniq_employee_schedule_date` and then crosses the D-082 notification boundary.

### K.1 Slice 12.6.3 readiness — three preconditions, not one

Slice 12.6.3 is **not** ready when D-091's reader evidence exists. All three of
these must be documented first:

1. **D-091** — the authoritative PHP helper and MariaDB schema behind
   `company_setting_selected_values(..., WEEKLY_OFF_DAYS)`, with its exact query,
   ordering, default and dirty-data semantics. No default may be invented.
2. **D-092** — the exact guard order for each of `check_in.php`,
   `check_in_qr.php` and `check_out.php`, showing where the fail-closed 403 sits:
   after the existing method, auth, company-active and required-body guards, and
   before any target-specific employee, session, location or branch read.
3. **D-093** — the coordinate edge-case matrix below, resolved against source.

### K.2 D-093 coordinate edge cases — to be resolved before 12.6.3

`required($body, [LATITUDE, LONGITUDE, METHOD])` runs first
(`check_in.php:16`); the geofence call is guarded separately at `:45`. The two
answer different inputs, and D-093 changes only the second. Each row states
which answers first; **the "after D-093" column is the intended behaviour and is
to be confirmed against source before implementation, not treated as settled by
this table.**

| `latitude` value | `required()` verdict | Legacy geofence invoked? | After D-093 |
|---|---|---|---|
| key absent | **fails** — `!isset` | never reached | unchanged: `field_required` |
| JSON `null` | **fails** — `isset(null)` is false | never reached | unchanged: `field_required` |
| `""` | **fails** — `=== ''` | never reached | unchanged: `field_required` |
| `"0"` | passes | **no** — `!empty("0")` is false | **yes** — validation runs |
| numeric `0` | passes | **no** — `!empty(0)` is false | **yes** — validation runs |
| `0.0` | passes | **no** — `!empty(0.0)` is false | **yes** — validation runs |
| non-numeric string (`"abc"`) | passes | **yes** — non-empty | unchanged: validation runs; `(float)` casts to `0.0` |
| valid positive (`30.0444`) | passes | yes | unchanged |
| valid negative (`-0.1276`) | passes | yes | unchanged |

Three points the matrix makes visible:

- The three genuinely-absent cases are all answered by `required()` **before**
  the geofence guard is reached, so D-093 cannot affect them. A missing
  coordinate still fails the same way it does today.
- D-093's entire effect is the three zero-valued rows.
- `"abc"` already reaches validation today and is cast to `0.0` by `(float)`,
  which means legacy already validates a garbage coordinate as Null Island while
  refusing to validate an honest one. That asymmetry is the clearest argument
  that the truthiness guard was never a deliberate rule.

## L. Route-inventory readiness

`LegacyPhpRouteInventoryTest` asserts 17 (12.4) + 15 (12.5) = **32** routes
bidirectionally, with five prefixes in `LegacyPhpRoutes.CONTROLLER_GUARDED`.
Wave 12.6 adds 15 + 3 = 18 for **50**, plus two prefixes. Per the Wave 12.5
convention each list is named for the slice that delivers it, with no empty
placeholder groups. **No routes are added by this document.**

## M. Cross-wave items that remain open

| Item | State |
|---|---|
| **D-083** | Open and materially more urgent. §F.4 shows the application clock deciding whether a row is auto-closed and what synthetic `check_out` is written, against `check_in` values the database wrote with `NOW()`. Not closed; no `SET time_zone` proposed. |
| **hr-platform#22** | Open; two endpoints reach the D-082 boundary. |
| **J2** | Unresolved; no `ORDER BY` invented anywhere. |
| **Item 13 / `company_settings`** | Frozen except for D-091's bounded read-only reader. |
| **Wave 12.7 / `requests`** | Reached by `overall_report` and `export` through the payroll helpers — a further reason those two are unscheduled. |
| **PostgreSQL / ETL** | Frozen and untouched. |
