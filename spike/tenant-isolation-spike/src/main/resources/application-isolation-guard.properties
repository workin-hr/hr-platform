# "isolation-guard" profile: only the common schema runs -- no RLS policy
# exists in this database at all. Branch access is enforced purely by
# GuardBranchService's explicit company_id-scoped queries.
spring.flyway.locations=classpath:db/migration/common
