# "isolation-rls" profile: RLS migration (db/migration/rls) runs in
# addition to the common schema. Branch access is enforced by Postgres
# Row-Level Security -- see RlsBranchService for the per-transaction
# session-variable set that RLS policy depends on.
spring.flyway.locations=classpath:db/migration/common,classpath:db/migration/rls
